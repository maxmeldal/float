import { LogEntry, LogLevel } from './logger-base';
export declare class ConsoleAppender<ComponentNamesT extends string> {
    #private;
    setLogLevel(defaultLogLevel: LogLevel): this;
    setLogLevel(componentLevels: {
        [key in ComponentNamesT]?: LogLevel;
    }): this;
    setLogLevel(defaultLogLevel: LogLevel, componentLevels?: {
        [key in ComponentNamesT]?: LogLevel;
    }): this;
    private includeInLog;
    levelToNumber(level: LogLevel): number;
    append(logEntry: LogEntry<ComponentNamesT>): void;
}
