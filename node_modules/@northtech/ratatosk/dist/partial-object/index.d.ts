/**
 * An object (typically configuration) which emits its data as an Observable as each parameter is set or changed.
 * This is useful for fetching and combining configuration data asynchronously, and works well with the Interpolated
 * strings.
 * @module
 */
export * from './partial-object';
