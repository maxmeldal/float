/**
 * Converts an ES6 Map to an object, returning undefined if the map was empty.
 */
export declare function mapToOptionalObject<T>(m: Map<string, T> | undefined): {
    [index: string]: T;
} | undefined;
/**
 * Converts an ES6 Map to an object, returning an object with no properties if the map was empty.
 */
export declare function mapToObject<T>(m: Map<string, T> | undefined): {
    [index: string]: T;
};
